/**
 * CSS identifier regex for custom property names
 * Matches valid CSS identifiers: alphanumeric, underscores, and hyphens
 */
const CSS_IDENTIFIER_REGEX = /^[\w-]+$/

export default CSS_IDENTIFIER_REGEX
