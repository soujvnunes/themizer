{
  "name": "ui-tokens",
  "version": "1.0.0",
  "description": "Give it an object reference and receive a flexible one with all CSS custom properties to be injected in the DOM the way you want.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [
    "design-system",
    "design-tokens",
    "CSS3",
    "Responsive"
  ],
  "author": "JV <soujvnunes@gmail.com> (https://soujvnunes.github.io)",
  "license": "ISC",
  "engines": {
    "node": ">= 16.0.0",
    "pnpm": ">= 8.0.0"
  },
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "lint": "eslint 'src/**/*.{ts,tsx}' --fix",
    "format": "prettier --write 'src/**/*.{ts,tsx,json,md}'",
    "test": "jest",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/jest": "^29.5.13",
    "@typescript-eslint/eslint-plugin": "^5.62.0",
    "@typescript-eslint/parser": "^5.62.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^8.10.0",
    "eslint-plugin-prettier": "^4.2.1",
    "jest": "^29.7.0",
    "prettier": "^2.8.8",
    "rimraf": "^3.0.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
